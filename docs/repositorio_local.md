# Explica como crear un repositorio local con comandos de git.

    Nos ubicamos en el repositorio desea, usando el comando cd para moverse a la carpeta donde se quiere trabajar.
    Se inicializa el repositorio este comando transforma el directorio en un repositorio de git ( git init)
    Agregamos el archivo se crea o se copia los archivos al directorio del repositorio, luego se añaden al area de preparacion 
    (git add)

    se hace el primer commit esto guarda un punto en la historia del proyecto.(git commit -m "commit tal tal") 

    se sube el repositorio y por ultimo para rectificar que si esta el commit ( git log --oneline)

